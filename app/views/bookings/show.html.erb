<%= @booking.id %>

<%= @booking.date_begin %>
<%= @booking.date_end %>
<%= @booking.user.email %>
<%= @booking.ride.name %>

<%= link_to "Edit", edit_ride_booking_path(@booking.ride, @booking) %>
<%= link_to "Delete", ride_booking_path(@booking.ride, @booking), method: :delete, data: { confirm: 'Are you sure?' } %>


<h1>Booking</h1>
<ul>
  <li>Status: <%= @booking.state %></li>
  <li>Item: <%= @booking.teddy_sku %></li>
  <li>Amount: <%= humanized_money_with_symbol(@booking.amount) %></li>
<ul>
